version: 2.1

jobs:
  test:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Validate
          command: ./test.sh

workflows:
  build:
    jobs:
      - test
